# 砲台ゲーム - 補習課題サンプル

## 概要
この課題では、`Base`クラスを継承して、砲台ゲームを作成します。
ベクトル、三角関数、加速度運動、当たり判定などの数学的概念を実際のゲームで確認できます。

## クラス構成

### Base クラス（親クラス）
- 位置 (`pos_`)
- 速度 (`vel_`)
- 色 (`Color_`)
- オブジェクトタイプ (`objType`)

### Cannon クラス（砲台）
- **固有の情報**
  - `angle_`: 砲台の角度（ラジアン）
  - `angleSpeed_`: 角度変化の速さ
  - `barrelLength_`: 砲身の長さ

- **操作方法**
  - 左キー: 砲台を左に回転
  - 右キー: 砲台を右に回転
  - スペースキー: 弾を発射

- **重要な処理**
  - `GetDirection()`: 角度から方向ベクトルを作成
    ```cpp
    return Math2D::FromAngle(angle_);
    ```
    これは `(cos(θ), sin(θ))` を計算しています

  - `GetTipPosition()`: 砲身の先端位置を計算
    ```cpp
    Vector2D dir = GetDirection();
    Vector2D offset = Math2D::Mul(dir, barrelLength_);
    return Math2D::Add(pos_, offset);
    ```

### Bullet クラス（弾）
- **固有の情報**
  - `radius_`: 弾の半径
  - `isActive_`: 弾が有効かどうか
  - `gravity_`: 重力の強さ

- **重要な処理（放物線運動）**
  ```cpp
  // 1. 重力を速度に加える
  Vector2D gravityVec = { 0.0f, -gravity_ };
  vel_ = Math2D::Add(vel_, gravityVec);
  
  // 2. 速度を位置に加える
  pos_ = Math2D::Add(pos_, vel_);
  ```
  
  これにより、弾は放物線を描いて飛びます。

### Target クラス（的）
- **固有の情報**
  - `radius_`: 的の半径
  - `isHit_`: 当たったかどうか

- **描画**
  - 3重の円で的を表現
  - 当たったら色が変わる

## 当たり判定の仕組み

弾と的の当たり判定は、円同士の衝突判定です：

```cpp
// 1. 弾と的の中心間の距離を計算
float dist = Math2D::Length(Math2D::Sub(bullet->GetPos(), target->GetPos()));

// 2. 衝突判定距離（半径の和）を計算
float collisionDist = bullet->GetRadius() + target->GetRadius();

// 3. 距離が衝突判定距離より小さければ当たり
if (dist < collisionDist)
{
    // HIT!
}
```

## 数学的な要点

### 1. 角度と方向ベクトル
角度 θ から方向ベクトルを作る：
```
方向ベクトル = (cos(θ), sin(θ))
```

### 2. 初速度の計算
弾の速度 = 方向ベクトル × 速さ
```cpp
Vector2D v = Math2D::Mul(dir, BULLET_SPEED);
```

### 3. 放物線運動
- 毎フレーム、速度に重力を加える
- 毎フレーム、位置に速度を加える

これにより、y方向の速度が徐々に変化し、放物線を描きます。

### 4. ベクトルの加算
位置の更新は、ベクトルの加算で表現：
```
新しい位置 = 古い位置 + 速度
```

### 5. ベクトルの長さ（距離）
2点間の距離は、差ベクトルの長さ：
```
距離 = √[(x?-x?)? + (y?-y?)?]
```

## 考察のポイント

1. **Baseクラスの役割**
   - どの情報をBaseクラスに置きましたか？
   - 各クラス固有の情報は何ですか？

2. **入力の使い分け**
   - なぜ角度変更には `IsKeepKeyDown()` を使いますか？
   - なぜ発射には `IsKeyDown()` を使いますか？

3. **方向ベクトルと速度ベクトル**
   - 方向ベクトルは長さ1です。なぜ便利ですか？
   - 速度ベクトルは方向ベクトルに速さを掛けたものです

4. **重力と放物線**
   - 重力は位置ではなく、まず速度に影響します
   - 速度が変わることで、位置の変化が曲がります

5. **当たり判定**
   - なぜ差ベクトルの長さが距離になりますか？
   - 半径が大きいほど当たりやすいのはなぜですか？

## 発展課題

余裕があれば以下を試してみましょう：

1. ターゲットを動かす
2. 複数のターゲットを配置
3. 弾数制限をつける
4. スコア表示を改善
5. 風の影響を追加（横方向の加速度）
6. 砲台の先端位置を行列変換で計算

## ファイル構成

```
Cannon.h / Cannon.cpp - 砲台クラス
Bullet.h / Bullet.cpp - 弾クラス
Target.h / Target.cpp - 的クラス
Stage.h / Stage.cpp   - ゲーム全体の管理
Base.h / Base.cpp     - 基底クラス
Math2D.h              - 2D数学ライブラリ
Input.h / Input.cpp   - 入力管理
```

## 操作方法

- **左右キー**: 砲台の角度を変更
  - 左キー: 角度を上げる（上向き）
  - 右キー: 角度を下げる（水平方向）
- **上下キー**: 弾の初速度を変更
  - 上キー: 速度を上げる（遠くへ飛ぶ）
  - 下キー: 速度を下げる（近くに落ちる）
- **スペースキー**: 弾を発射
- **ESCキー**: ゲーム終了

## ゲームの目標

画面左側の砲台から弾を発射し、画面右側の的に当てよう！
角度と速度を調整して、放物線運動をうまく使って命中させましょう。
